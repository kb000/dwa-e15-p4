 //
 // Automate some form control synchronization.
 //
 
 $( document ).ready(function() {
            // $('#foo_id').change(function() {
            //    $('#bar_id').val($(this).val());
            //});
        });